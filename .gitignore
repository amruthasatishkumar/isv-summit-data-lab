# OS / editor junk
.DS_Store
Thumbs.db
desktop.ini
*.swp
*~

# VS Code workspace state
.vscode/
*.code-search

# Copilot / AI tool state
.copilot/
.github-copilot-cache/

# Local secrets (defensive — none expected in this repo, but just in case)
*.env
.env.*
!.env.example
secrets/
credentials/

# Build artifacts (none today, but future-proof)
node_modules/
dist/
build/

# Local-only authoring tools (paste-to-disk screenshot uploader, dev server)
dev-tools/
scripts/relocate-screenshots.ps1

# Day 1 RTI working folder (local only, not part of the published lab)
Day 1 RTI/

# Python bytecode from seed-data scripts
__pycache__/
*.py[cod]
